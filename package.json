{
  "name": "resolution-tracker",
  "version": "1.0.0",
  "description": "AI-powered personal resolution tracker for 2026",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:js && npm run build:css",
    "build:js": "npx esbuild resolution-tracker.jsx --bundle --outfile=dist/app.js --jsx=automatic --jsx-import-source=https://esm.sh/react",
    "build:css": "echo 'CSS is inline in component'",
    "serve": "npx serve dist",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "keywords": [
    "resolution",
    "tracker",
    "ai",
    "goals",
    "react"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.19.0",
    "serve": "^14.0.0",
    "gh-pages": "^6.0.0"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "homepage": "https://yourusername.github.io/resolution-tracker"
}